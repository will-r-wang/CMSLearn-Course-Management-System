<div class="card">
  <div class="card-body">
    <h5 class="card-title">User Registration Requests</h5>
    <p class="card-text">Your User Registration Request is <%= @user_registration.status %>. Please check the table below for updates:</p>
    <table class="table table-sm">
      <thead class="thead-dark">
        <tr>
          <th scope="col">#</th>
          <th scope="col">Account Type</th>
          <th scope="col">Status</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th scope="row"><%= @user_registration.id %></th>
          <td><%= @user_registration.user_type %></td>
          <td><%= @user_registration.status %></td>
        </tr>
      </tbody>
    </table>
    <p class="card-text"><small class="text-muted">Last refreshed at <%= DateTime.now.strftime '%Y-%m-%d %H:%M:%S' %></small></p>
  </div>
</div>
<br><br>
